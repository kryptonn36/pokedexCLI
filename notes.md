bufio.NewScanner(os.Stdin)      ==   it creates a new scanner so that input can be stored in a variable
output.scan()                   ==   it will take strings input from terminal and read it one by one 
